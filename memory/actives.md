# Active Memory

Generated by `scripts/quant_memory_evolve.py` at `2026-07-16 20:46:45`.

Read this file at the start of each session after `memory/profile.md`.

<!-- memory:auto:start -->

## Session Boot Context

- Default profile remains `quality_regime` unless promotion review explicitly approves a change.
- 20-day P2 shadow is warning evidence only. 60/90/120-day P2 windows are the main promotion evidence.
- Research OOS strength does not justify promotion without P2 execution parity.
- Do not treat lower drawdown from low invested weight as alpha.
- Main profile: `quality_regime`.

## Reusable Strategy And Execution Cards

### A1. P2 reserve re-optimization can silently overrun upstream target weights
- source: `errors.md` `ee42163aac326712`
- tags: p2, target_weight, reserve_pool, promotion
- confidence: unspecified
- evidence: v16 profile daily for `2026-04-02` correctly capped upstream target weight to `18%`, but P2 initially re-optimized the next trade day back to about `38.6%` because it dropped target-weight columns before pretrade. After the fix, 2026-04-03 P2 target stayed at `18%`, 2026-04-07 trapped sell weight fell from about `35.7%` to `14.4%`, and `upstream_target_weight_overrun_max=0`.
- action: When a daily signal file carries positive `target_weight`, derive P2 `total_target` and `max_single` from the upstream weights before any pretrade/reserve re-optimization. Rolling replay must report `upstream_target_weight_overrun_max`, and promotion must hard-fail positive overrun.
- takeaway: Do not interpret profile guard failures until target-budget lineage is proven.

### A2. Sell-trap clusters can be hidden by aggregate window metrics
- source: `errors.md` `804a2848958e0a57`
- tags: sell_trap, promotion, p2, a_share
- confidence: unspecified
- evidence: v11 review added `halt_cluster_sell_trap_worse_than_main` and `halt_cluster_severe_sell_trap_failed` for v9 after merging halt attribution; v9 had severe 2026-04-07 style trapped sell exposure despite some windows passing NAV/MDD parity.
- action: Promotion review must consume halt-cluster attribution and hard-fail severe sell-trap days, not only aggregate blocked order totals.
- takeaway: Do not rely only on 60/90/120 aggregate NAV and MDD when A-share sell traps cluster on specific trade dates.

### A3. P2 replay can become invalid if a script-level rebalance path recomputes target weights
- source: `errors.md` `3622f3b98422ad92`
- tags: p2, target_weight, promotion
- confidence: unspecified
- evidence: External expert review found that legacy `_rebalance` code in `scripts/quant_p2_paper_trade.py` could overwrite optimizer weights and double-apply total position.
- action: Use `core.execution.paper_broker.PaperBroker` as the canonical rebalance path and require tests proving external `target_weight` is preserved.
- takeaway: Rolling replay and promotion gates become unreliable if P2 requested weights are not the same weights produced by the portfolio layer.

### A4. Execution limits must consume official pre-close and limit prices
- source: `errors.md` `e3019ff439273b18`
- tags: execution, price_limits, corporate_action, a_share
- confidence: unspecified
- evidence: `load_execution_bars` constructs `prev_close` from prior raw close, while `PaperBroker` infers locked limits from a fixed ratio. This can misclassify ex-rights days and status/board transitions even though ODS already provides official daily limit fields.
- action: Use ODS `pre_close`, `up_limit`, and `down_limit` for execution tradability. Fixed board/ST ratios are fallback-only when official fields are absent.

### A5. P2 profile cap bugs make old reserve/exit-trap artifacts legacy evidence
- source: `errors.md` `4b41aa7a0fa41f82`
- tags: p2, reserve_pool, exit_trap, promotion
- confidence: unspecified
- evidence: v27 work added focused tests proving a positive 2% reserve cap constrains active reserve target weight and that exit-trap threshold/total/single caps flow from profile config into the P2 target-weight path. This closes a risk where P2 replacement could silently ignore or zero-clamp profile caps.
- action: Treat P2 artifacts generated before the reserve/exit-trap cap fix as old-cap evidence. Before using a reserve-cap or exit-trap-cap profile for promotion arguments, rerun P2 with tests proving `_assign_profile_target_weights` consumes `target_max_reserve_weight`, `target_exit_trap_risk_threshold`, `target_max_exit_trap_weight`, and `target_max_exit_trap_single_weight`.
- takeaway: Do not compare cap-sensitive profiles across a cap-lineage break.

### A6. Invested-average style gates can manufacture low-risk cash exposure
- source: `errors.md` `b1f22b18a20137c4`
- tags: pretrade, style_exposure, p2, cash_drag
- confidence: unspecified
- evidence: v18's invested-average style gate blocked nearly all candidates as `style_single_gate_collapse`, with 20-day target-weight mean about `2.24%` and `16` executable-pool halt days. v23's NAV-weighted style basis reduced halt days to `0` and target-weight mean rose to about `36.65%`, but NAV/MDD worsened to about `-3.14%/-4.15%`.
- action: Use NAV-weighted style exposure for execution-credibility diagnostics, and require target-weight utilization plus NAV/MDD parity before treating a profile as better. If using invested-average style exposure, label it as a defensive risk-off guard rather than alpha evidence.
- takeaway: An execution gate that prevents most entries can hide weak alpha.

### A7. Same row count can hide mismatched P2 evidence calendars
- source: `errors.md` `9427c1f1cf410cd5`
- tags: signal_calendar, p2, promotion, evidence_lineage
- confidence: unspecified
- evidence: v22 had 65 replayable profile daily files and passed the count-based 60-day coverage check, but the candidate's 60-day P2 window started at `20251216` while the main profile started at `20251223`. The candidate was missing the main reference dates `20260126,20260127,20260128,20260203,20260205`; after calendar parity was added, it was rejected before P2 spend.
- action: Promotion refresh must require candidate profile daily files to cover the main profile's replayable reference signal dates, not just any N replayable files. Use the signal-coverage precheck's `missing_reference_signal_days` and `profile_signal_calendar_mismatch` reason before running P2 smoke or long windows.
- takeaway: Do not compare candidates that fill missing dates by shifting the window backward.

### A8. Score-alpha and coverage pass can still fail P2 execution smoke
- source: `errors.md` `e4c3c4cd6fdb5c30`
- tags: score_gate, signal_coverage, p2, promotion
- confidence: unspecified
- evidence: After rebuilding v22 profile daily files to 65 replayable days, v22 passed score-alpha and signal-coverage prechecks. Its 60-day in-process P2 smoke still lost to the main profile: `quality_regime` NAV/MDD `+1.18%/-5.89%`, while `quality_regime_candidate_v22_relaxed_low_volume_gate` was `-1.91%/-7.71%`; v22 also had `7` exit-not-tradable orders versus main `3`. The new P2 smoke precheck correctly skipped v22 before 90/120 spend.
- action: Treat score-alpha and signal-calendar coverage as prerequisites only. Run a same-generation 60-day P2 smoke before 90/120 refresh, and skip candidates whose NAV, MDD, target-weight utilization, or broker tradability blocks are worse than the main profile.
- takeaway: Do not mistake final-ranking alpha smoke or signal coverage for execution pass-through.

### A9. Score-alpha pass can hide profile daily coverage gaps
- source: `errors.md` `defc2d7f975d27f4`
- tags: score_gate, p2, coverage, promotion
- confidence: unspecified
- evidence: The 2026-04-28 survivor smoke kept v14/v15/v17/v22 after score-alpha precheck, but P2 reported only 10 signal days for v14/v15 and 20 signal days for v17/v22, while main profile had 60 shared signal days. The new signal-coverage precheck later rejected those survivors before P2 spend: v14/v15 `10/60`, v17/v22 `20/60`, leaving only `quality_regime`.
- action: Before long-window P2 or promotion review, require profile-isolated daily signal coverage to be comparable to the requested evidence window. Use `quant_refresh_promotion_gate.py --precheck-signal-coverage`; a candidate with only 10-20 signal days must not be described as 60-day evidence even when replay uses `window=60`.
- takeaway: Score-alpha can be valid on the rows it sees but still be too sparse for promotion evidence.

### A10. Shared daily files can be stale/orphan artifacts when market bars are missing
- source: `errors.md` `750f1097d77c800a`
- tags: daily_signal, data_quality, p2, promotion
- confidence: unspecified
- evidence: `output/daily` had daily files for `20260126`, `20260127`, `20260128`, `20260203`, and `20260205`, while the main market parquet had zero rows for those dates. These files initially made v22 look like it missed main reference dates. After filtering signal dates by market-date membership, v22 passed calendar coverage but still failed clean 60-day P2 smoke on NAV, MDD, and exit-not-tradable parity.
- action: Rolling replay and promotion reference calendars must require signal dates to exist in `data/daily_all_5y.parquet`. Exclude shared or profile daily files whose signal date has no market bars, and record the exclusion as stale/orphan signal handling rather than candidate failure.
- takeaway: Do not use `output/daily` as the market calendar.

### A11. Target-score overrides can reduce losses by starving deployment
- source: `errors.md` `d58c1a9eb1723810`
- tags: target_score_col, cash_drag, p2, promotion
- confidence: unspecified
- evidence: v21 used `ml_score` for target weights and improved 20-day NAV/MDD versus v18, but target_weight_sum_mean fell to about `18.3%`, risk block rate rose to about `76.6%`, executable-pool halt days rose to `3`, and objective remained weak.
- action: Treat target-score experiments as failed unless they pass both score-alpha smoke and target-weight utilization floors. Compare objective, target_weight_sum_mean, risk block rate, and executable-pool halt days before celebrating NAV/MDD improvement.
- takeaway: If a profile loses less because it invests less, it has not solved the alpha problem.

### A12. BJ-only shared daily files can masquerade as repeated P2 failures
- source: `errors.md` `7247b085026f5410`
- tags: daily_signal, p2, quality_regime, promotion
- confidence: unspecified
- evidence: v10 inspection showed `output/daily/daily_20260319.csv` and `daily_20260320.csv` each had 10 raw rows, all `920xxx`; with default BJ/9-code exclusion they became `empty_after_universe_filter`, not physical empty files.
- action: Promotion refresh should classify raw empty files and post-universe empty files as explicit no-signal ledger states with separate coverage metrics.
- takeaway: Do not mix BJ-only/empty-after-universe-filter days with executable-pool halt or broker tradability blocks.

<!-- memory:auto:end -->

## Manual Notes

- Keep this section for human notes that should not be overwritten by the generator.
- 2026-05-07 carry-forward: v25/v26 are negative boundary evidence, not next promotion candidates. v25 liquidity target-score passed the old relative score-alpha gate but 60-day P2 mostly lost less by investing less (`target_weight_sum_mean` about `14.7%` vs main `29.5%`). v26 removed the style-cash trap with NAV-weighted style exposure, but after disabling capacity-aware unconstrained fallback it produced valid zero-target/no-entry evidence and failed the hardened score-alpha gate because the top bucket forward return was still negative in absolute terms. Do not spend 90/120 P2 on v25/v26 unless a rebuilt candidate first passes positive top-bucket score-alpha, coverage, and target-weight utilization checks.
